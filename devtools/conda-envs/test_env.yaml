name: test
channels:

  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # Package dependencies
  - numpy
  - matplotlib
  - networkx

  # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov